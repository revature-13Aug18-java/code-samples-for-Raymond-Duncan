import { ReimbTypePipe } from './reimb-type.pipe';

describe('ReimbTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ReimbTypePipe();
    expect(pipe).toBeTruthy();
  });
});
